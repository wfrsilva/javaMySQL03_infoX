-- cria banco de dados
-- create DATABASE dbinfox;
-- use dbinfox;
create table tbusuarios (
idsuer int primary key,
usuario varchar(50) not null,
fone varchar (15),
login varchar(15) not null unique,
senha varchar (15) not null
);

describe tbusuarios;